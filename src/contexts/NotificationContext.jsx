// --- src/contexts/NotificationContext.jsx ---
import { createContext } from 'react';

// Sadece bildirimi tutacak olan Context'i oluştur ve ihraç et
export const NotificationContext = createContext(null);